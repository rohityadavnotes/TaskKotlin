package com.task.constants

import com.task.BuildConfig

object AppConstants {
    const val APP_NAME                               = "Task"
    const val PACKAGE_NAME                    = BuildConfig.APPLICATION_ID
}